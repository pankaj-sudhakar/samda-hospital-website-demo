/*
 * Public read-only Sanity configuration.
 * Add the project ID only after the hospital's Sanity project and CORS origins
 * have been configured. Never place an API token in this file.
 */
window.SAMDA_SANITY_CONFIG = {
  projectId: '',
  dataset: 'production',
  apiVersion: '2026-09-08'
}
